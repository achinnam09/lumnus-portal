// This is the internal dashboard that reviewers will use to view applicants and filter data

const Dashboard = () => {
    return (
        <div>
            <h2>Dashboard Page</h2>
        </div>
    );
};

export default Dashboard;